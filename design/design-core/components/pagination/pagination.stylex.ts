import stylex from "@stylexjs/stylex";

export const styles = stylex.create({
  pagination: {},
  pagination$ellipsis: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    verticalAlign: "super",
  },
});
