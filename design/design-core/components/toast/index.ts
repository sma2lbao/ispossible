import { info, success, warning, error, close, destroyAll } from "./toast.api";

const ExportToast = {
  info,
  success,
  warning,
  error,
  close,
  destroyAll,
};

export { ExportToast as Toast };
