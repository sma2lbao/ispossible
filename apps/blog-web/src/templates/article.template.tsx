import React from "react";
import { Paper } from "@mui/material";

interface ArticleTemplateProps {}

const ArticleTemplate: React.FC<ArticleTemplateProps> = (props) => {
  return <Paper>页面模板</Paper>;
};

export default ArticleTemplate;
